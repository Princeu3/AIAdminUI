# AI Admin UI Backend Environment Variables

# App settings
DEBUG=true
APP_NAME=AI Admin UI

# Security (generate these for production)
SESSION_SECRET=your-session-secret-here
JWT_SECRET=your-jwt-secret-here
ENCRYPTION_KEY=your-encryption-key-here

# GitHub OAuth (create at https://github.com/settings/developers)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_REDIRECT_URI=http://localhost:8000/api/auth/github/callback

# Claude Code Configuration
# For local dev: Claude uses ~/.claude/ credentials automatically
# For Docker/Railway: Provide credentials as JSON strings (base64 decoded)

# Option 1: Copy contents of ~/.claude/.credentials.json
CLAUDE_CREDENTIALS_JSON=

# Option 2: Copy contents of ~/.claude/settings.json (optional)
CLAUDE_SETTINGS_JSON=

# Skip permission prompts in Docker/unattended mode
CLAUDE_CODE_BYPASS_ALL_PERMISSIONS=1

# Vercel (optional - for preview URLs)
VERCEL_TOKEN=
VERCEL_TEAM_ID=

# Database
DATABASE_URL=sqlite+aiosqlite:///./aiadmin.db

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Repos storage
REPOS_BASE_PATH=/tmp/repos
